import { Button, Hero, HeroBody, HeroSection, HeroTitle, HeroTeaser, HeroActionList, HeroAction, ScreenContainer } from 'smooth-doc/components';
import { Link } from 'gatsby';
import heroBackgroundURL from "../../../../images/hero-background.png";
import * as React from 'react';
export default {
  Button,
  Hero,
  HeroBody,
  HeroSection,
  HeroTitle,
  HeroTeaser,
  HeroActionList,
  HeroAction,
  ScreenContainer,
  Link,
  heroBackgroundURL,
  React
};